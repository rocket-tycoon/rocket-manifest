[package]
name = "feature_panel"
version = "0.1.0"
edition = "2024"

[dependencies]
gpui.workspace = true
gpui-component.workspace = true
manifest_client = { path = "../manifest_client" }
uuid = { version = "1.0", features = ["serde"] }
